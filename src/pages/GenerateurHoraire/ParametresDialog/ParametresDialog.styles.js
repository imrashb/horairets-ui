import styled from 'styled-components';

const ParametresDialogWrapper = styled.div`

    & > * {
        &:not(:first-child) {
            margin-top: 1rem !important;
        }
    }

`;

export default ParametresDialogWrapper;
